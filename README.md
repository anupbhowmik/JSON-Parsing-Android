# JSON Parsing Android Starter

A good starting point for fetching `JSON` data and show in Android
- Fetches images and details from `pixabay.com`
- Shows a list in `RecyclerView`
- Uses `MVC` Architecture
